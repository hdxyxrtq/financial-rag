## 性能对比（Benchmark）— V7

> 由 `scripts/benchmark.py` 自动生成。

### 本轮修改（相比 V6）

| 参数                 | V6 值                                                          | V7 修改后                                                                       |
| ------------------ | ------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| Reranker min_score | 0.3                                                           | 0.0（不过滤，纯模型排序）                                                               |
| Reranker 加载方式      | `CrossEncoder("BAAI/bge-reranker-v2-m3")` 直连 Hub（SSL 失败时静默降级） | 优先从本地快照路径加载，绕过 HuggingFace Hub 网络请求                                          |
| 其他参数               | 不变                                                            | 同 V6（严格 Prompt、47 chunks、temperature 0.1、hybrid score_threshold=0.3、top_k=8） |

> ⚠️ V7 首次运行时因 SSL 证书验证失败导致 BGE reranker 静默降级（50 条 query 全部降级为原始排序），数据无效。修复 `local_reranker.py` 后重跑，本轮为 reranker 真正生效的 min_score=0.0 结果。

### 结论

BGE reranker min_score=0.0（不过滤，纯重排序）：忠实度提升 1.4%、精确度提升 3.6%、召回率提升 5.9%，答案相关性仅降 1.2%。相比 V6（min_score=0.3）的精确度 +5.7%、相关性 +15.6% 但召回率 -3.8%，V7 的 min_score=0.0 在召回率上有明显优势（0.8411 vs 0.7870），且忠实度更高（0.8834 vs 0.8212）。延迟方面，本地 BGE 推理 AVG=10.4s，比 V6 的 AVG=88s（API reranker）大幅缩短。综合来看，min_score=0.0 是更均衡的配置。

### RAGAS 评估指标

| 配置                | 忠实度    | 答案相关性  | 上下文精确度 | 上下文召回率 |
| ----------------- | ------ | ------ | ------ | ------ |
| vector            | 0.8322 | 0.3950 | 0.6625 | 0.8296 |
| bm25              | 0.8203 | 0.4132 | 0.7171 | 0.8372 |
| hybrid            | 0.8716 | 0.3909 | 0.7513 | 0.7946 |
| hybrid + Reranker | 0.8834 | 0.3861 | 0.7783 | 0.8411 |

### 检索延迟

| 配置                | P50 (ms) | P95 (ms) | AVG (ms) |
| ----------------- | -------- | -------- | -------- |
| vector            | 3878     | 10902    | 4756     |
| bm25              | 2733     | 8166     | 3712     |
| hybrid            | 3400     | 9683     | 4329     |
| hybrid + Reranker | 9144     | 18257    | 10406    |

### 关键结论

- **混合检索 vs 纯向量** — 忠实度（faithfulness）：提升 4.7%（0.8322 → 0.8716）
- **混合检索 vs 纯向量** — 答案相关性（answer_relevancy）：下降 1.1%（0.3950 → 0.3909）
- **混合检索 vs 纯向量** — 上下文精确度（context_precision）：提升 13.4%（0.6625 → 0.7513）
- **混合检索 vs 纯向量** — 上下文召回率（context_recall）：下降 4.2%（0.8296 → 0.7946）
- **Reranker 精排效果** — 忠实度（faithfulness）：提升 1.4%（0.8716 → 0.8834）
- **Reranker 精排效果** — 答案相关性（answer_relevancy）：下降 1.2%（0.3909 → 0.3861）
- **Reranker 精排效果** — 上下文精确度（context_precision）：提升 3.6%（0.7513 → 0.7783）
- **Reranker 精排效果** — 上下文召回率（context_recall）：提升 5.9%（0.7946 → 0.8411）
- **混合检索延迟**：P50 = 3400ms，满足交互体验要求（< 3s）
- **推荐配置**：hybrid + Reranker（综合得分最高）
